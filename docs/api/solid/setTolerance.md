# `Solid.setTolerance()`

Returns a new `Solid` rebuilt from `Manifold.setTolerance(tolerance)`.

## Usage

```js
const tolerant = solid.setTolerance(0.02);
```

Use this when you need a new solid built with a different boolean robustness tolerance.
