# `Solid.removeSmallInternalIslands()`

Convenience wrapper that removes only small internal islands below a triangle-count threshold.

## Usage

```js
solid.removeSmallInternalIslands(15);
```

Use this when you want to preserve exterior fragments but eliminate trapped interior junk.
