# `Solid.invertNormals()`

Flips all triangle winding, reversing the solid's normals. The solid is mutated and returned.

## Usage

```js
solid.invertNormals();
```

Use this when a mesh is consistently inside-out.
